import React from "react";
import Home from "./components/Home";

export const metadata={
  title:"Home task",
};

const page = () => {
  return (
    <>
      <Home />
    </>
  );
};

export default page;
